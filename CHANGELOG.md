# 📜 開発履歴 ＆ 変更推移完全ログ（CHANGELOG.md）

本プロジェクト（2126年 架空iPadOS型 脱出ゲームシステム `26__0094`）における、これまでの全指示・全改修・変更推移の記録です。

---

## 📅 更新履歴・変更推移一覧

### 【v2.12.0】指示書台帳（指示.md）創設 ＆ 30台リアルタイムランキング（リーダーボード）実装
* **📋 指示書台帳（`指示.md`）の創設と運用ルール確立**:
  * これまでの全指示・プロンプト・ルール・仕様要件を時系列および機能別に体系的に集約したマスター指示書（`指示.md`）を作成。
  * 編集時・応答時に過去の指示が厳格に守られているか、既存機能が損なわれていないかを毎回自己点検するセルフチェックリストを策定。
* **🏆 30台 リアルタイム進行度ランキング（リーダーボード）新設**:
  * 管理画面（admin.html）の進行モニタリングに「📱 グリッド」と「🏆 ランキング」の切り替えタブを設置。
  * 到達周回・入手情報数・manabaログイン状況から総合スコアをリアルタイム算出し、1位〜3位（🥇🥈🥉）のメダルバッジおよびプログレスバー付きランキングテーブルを表示。

### 【v2.11.0】全30台一括マスターデータリセット ＆ 進行モニタリング通信強化
* **🚨 全30台 一斉マスターデータリセット（本番前初期化機能）**:
  * 管理画面（admin.html）の「公演前 一括マスターデータリセット」ボタンから、**稼働中の全30台のiPadを一斉に初期状態（1周目・ロック画面・データ全消去）へ初期化**し、スプレッドシートのログも完全クリア。
* **iPad単体での完全初期化ボタン**:
  * 電話アプリで `**##**` を入力して開くスタッフ設定画面に「⚠️ この端末のデータを完全初期化 (全消去)」ボタンを設置。
* **30台リアルタイムモニタリング通信のGET/POSTハイブリッド化**:
  * iPadからのステータス送信（進捗ハートビート）をGETパラメータ化し、CORS（通信遮断）を100%回避して確実にスプレッドシートおよび管理画面に反映。
* **管理画面モニタリングのマルチティア・フォールバック取得**:
  * `action=get_status` および `action=get_data` の両方から全30台の進捗を自動マージして描画。

### 【v2.10.0】システム呼び出しの「**##**」完全一致化 ＆ 時計タップ無効化
* **システム呼び出しを「`**##**`」（6文字完全一致）に限定**:
  * 途中の誤爆を防ぐため、電話アプリで「`**##**`」が入力された瞬間のみ厳密にシステム画面が起動するように改修。
* **「時間タップで開く」挙動の完全削除**:
  * ステータスバー時計5回タップの隠しリスナーを完全無効化・削除。
* **スタッフ画面に「🔄 端末を完全再読み込み (リロード)」ボタンを常設**:
  * キャッシュクリア・画面完全リフレッシュをワンタップで実行可能。

### 【v2.9.0】電話アプリ隠しコマンド「**##**」＆ スタッフ画面リロードボタン実装
* **電話アプリからの隠しシステム呼び出し（`**##**`）**:
  * 電話アプリのキーパッドで「`**##**`」を入力（または発信）すると、ファンファーレ音とともに**スタッフ用システム設定画面**が即座に起動。
* **スタッフ画面に「🔄 端末を完全再読み込み (リロード)」ボタンを新設**:
  * iPad端末のキャッシュクリア・完全再読み込みをワンタップで実行可能に。
* **「時計5回タップ」も引き続き併用可能**:
  * 右上時計5回タップと電話アプリ `**##**` の両方からスムーズにセットアップ画面を呼び出し可能。

### 【v2.8.0】クラウドリアルタイム遠隔統制 ＆ 30台接続診断システム
* **クラウド経由の遠隔コマンド同期（Google Apps Scriptハブ）**:
  * 管理画面（admin.html）から送信したシーン進行プリセット、効果音・サイレン、全画面緊急アラートが、スプレッドシート（`98_運営コマンドキュー`）を介して**別ネットワークにある30台のiPadへリアルタイムに一斉配信・実行**される完全なクラウド遠隔統制を実装。
* **管理画面（admin.html）「📡 クラウド遠隔統制ステータスバー」の常設**:
  * 画面最上部に接続状態インジケーター（🟢 正常 / 🔴 エラー）、Ping疎通テストボタン（応答時間ms表示）、直近送信コマンド履歴を完備。
  * 4秒おきに30台の生進行状況（周回、ヒント数、manaba状況、最終通信時間）を自動同期。
* **iPad側スタッフ画面（時計5回タップ）の通信診断インジケーター**:
  * クラウド通信の正常性（🟢 正常・応答速度 / 🔴 エラー詳細）をリアルタイム表示し、どこで通信が止まっているかを一目で特定可能に。

### 【v2.7.0】LINKアプリ内 Googleフォーム常時閲覧 ＆ リッチリンクカード実装
* **トークルーム上部「📌 共有フォーム」固定ピン留めバナーの設置**:
  * LINKアプリのトーク画面上部に、常時アクセス可能な「共有ファイル: 2126年 メンタルヘルス・スキャン（開く ↗）」ピン留めバナーおよびショートカットボタンを設置。
* **深澤・グループチャット内のリッチGoogleフォームカード（LINE風UI）**:
  * 1周目・2周目のトークメッセージ内に、紫色のGoogle Forms風リッチリンクカード（バッジ・タイトル・説明・開くボタン）を配置し、ワンタップで偽Googleフォームを起動可能に。
* **Googleフォーム画面の「✕ 閉じる」ボタン追加**:
  * フォーム閲覧後にワンタップでスムーズにLINK画面へ復帰できる導線を整備。

### 【v2.6.0】iPad 30台同時運用対応 ＆ 自由ユーザー名システム
* **iPad 30台（iPad-01〜30）および自由ユーザー名対応**:
  * 端末識別子として「iPad-01」〜「iPad-30」のほか、「1班 田中」「チームAlpha」など任意のユーザー名・班名を完全サポート。
* **スタッフ用隠し設定モーダル（時計5回タップ）の刷新**:
  * 01〜30 のクイック番号選択ボタングリッドを配置。
  * 自由なユーザー名入力欄および周回（1〜3周目）強制設定ボタンを完備。
* **運営用管理画面（admin.html）の30台リアルタイム監視ダッシュボード化**:
  * 30台の進行状況（周回バッジ、manabaログイン、入手情報数、最終通信時間）を一画面でグリッド監視。
  * 周回別フィルター（全30台 / 1周目 / 2周目 / 3周目）およびユーザー名・端末番号のリアルタイム検索バーを搭載。

### 【v2.5.0】Googleスプレッドシート連携 ＆ ゲームマスターガイド策定
* **Google Apps Script (GAS) Web API連携**:
  * ユーザー作成のGASエンドポイント（`https://script.google.com/macros/s/AKfycbwKAWMjn0ywOYor7_EQ63HDyoxw_Ag5gH81Efs45ttVKa3vdi6HyOveZrBADpkycIpaYw/exec`）と本番接続。
  * アプリ起動時にスプレッドシートから最新データを非同期取得し、画面を自動更新する `fetchLatestDataFromSpreadsheet()` を実装。
  * プレイログおよび進行状況のリアルタイム記録（`write_log`, `update_status`）を本番稼働。
* **ワンクリック全シート自動生成スクリプト（`code.gs`）の整備**:
  * 全22記事（各3,000〜4,600字）を含む全10シートを一発で自動構築する `setupDatabaseSheets()` を配備。
* **manabaアプリのユーザー名描画バグ修正**:
  * `manaba-user-display` / `manaba-user-name` のID不一致による null 参照エラーを解消し、nullガードを強化。
* **ゲーム完全マスターガイド（`GAME_MASTER_GUIDE.md`）の策定**:
  * 全パスワード（`25B1150`, `JNNITMNR`, `119.43 MHz`）、全学籍ID/Pass、QRコード、全10ツールの操作トリガーと発生イベントを完全体系化。

### 【v2.4.0】LINK QRコードリーダーの独立起動 ＆ 正方形ビューファインダー化
* **LINK専用QRリーダーの独立化**:
  * LINKアプリのヘッダーにある「友達追加（`user-plus`）」から、メタアプリを介さず直接専用QRスキャナーモーダル（`#link-qr-modal`）が起動するよう改修。
  * QRコード読み取り成功時にファンファーレ音とともに相手とのトークルームを自動オープン。
* **正方形ビューファインダー（260px × 260px）の作成**:
  * 細長くなっていたスキャナー枠を正方形に統一。
  * 四隅に光るグリーン（`#34c759`）のL字コーナーボーダーと、上下に往復するリアルなレーザースキャンアニメーション（`qr-laser-line`）を実装。
  * メタアプリ側のQRスキャナーも正方形に統一。

### 【v2.3.0】画面スクロール・オーバースクロール・Pull-to-refresh完全防止
* **CSSによる画面固定**:
  * `html, body` に `position: fixed; width: 100vw; height: 100vh; overflow: hidden; overscroll-behavior: none; touch-action: none;` を設定。
* **JavaScriptによる引っ張りリロード（Pull-to-refresh）の完全ブロック**:
  * スクロール可能要素以外のドラッグを全面禁止。
  * スクロール要素内であっても最上部（`scrollTop === 0`）での下向き引っ張り操作を検知して `e.preventDefault()` を実行し、Safari/Chromeの再読み込みを100%遮断。
  * ニュース記事本文やLINKチャット等の内部スクロールはスムーズに動作を維持。

### 【v2.2.0】1周目における「鵜沢」検索結果の非表示＆伏線化、全周回のネタバレ・矛盾完全解消
* **1周目「鵜沢」の完全隠蔽**:
  * 検索インデックスの「鵜沢」「うざわ」「鵜沢向希」「向希」に `minLoop: 2` を設定し、1周目はヒット0件に設定。
  * 1周目のトップニュースHero記事を「東金市在住の向希（こうき）さん（119歳）」と名字を伏せる形に変更。
* **1〜3周目における全情報の整合性強化**:
  * 「時価総額」「ランキング」「U.Z.W.」「uzw」「ハッキング」を3周目のみヒットするように設定。
  * 「学友会」は 1〜2周目で現役HP、3周目で「35年前解散」記事へ切り替え。
  * LINKチャットの犬飼の「鵜沢向希様」脅迫メッセージを3周目のみ表示するフィルターを実装。
  * 2周目の深澤チャットから怪しいメンタルヘルス・スキャン（偽Googleフォーム）への導線を追加。

### 【v2.1.0】全22記事の超本格リッチ化（各3,000〜4,600文字）
* **高齢者の生活・感情・社会背景に迫る本格ジャーナリズム記事化**:
  * 記者の潜入取材、高齢者（119歳の向希さん）へのインタビュー、貧しい日常、健康法、配給制度の崩壊などを詳細に執筆。
  * 1章・2章のような不自然な章立てを廃止し、見出し・小見出し・重要発言引用・データ解説のYahoo!ニュース風レイアウトへ刷新。
  * 記事内の「📝 調査メモに転記」ボタンで重要キーワードをメタアプリへ自動記録。
  * ロック画面の通知バナーから各記事へ直接ジャンプできるディープリンクを実装。

### 【v2.0.0】ハッキング画面のUI改善（編集アイコン導入）
* **Googleフォームの「編集権限をリクエスト」をペンのアイコン（マーク）に変更**:
  * Googleフォーム右上にペンの編集アイコンを配置し、タップすることで機密スプレッドシートへ自然に侵入できるUIへ改修。

### 【v1.0.0】初期リリース ＆ 基本システム構築
* iPadOS風UI（ロック画面、パスコード解除 `25B1150`、ステータスバー、Dock、マルチタスク風アプリアイコン）
* Googleブラウザ、LINK、Mail、Phone、Settings、manaba、26__0094（メタアプリ）の実装
* 運営用管理画面（`admin.html`）およびリアルタイム監視・プリセット演出機能の実装
